# Local env — never commit real credentials.
.env
.env.local

# TanStack Router generated route tree (regenerated by `vite dev` / `vite build`).
src/routeTree.gen.ts

# Build / deploy output.
.output/
.nitro/
.tanstack/
.vercel/
.vinxi/

# Better Auth CLI generated migration.
better-auth.schema.sql
